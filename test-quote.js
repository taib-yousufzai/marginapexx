require('dotenv').config({path: '.env.local'}); const { getSharedKiteSession } = require('./lib/kiteSession.ts'); getSharedKiteSession().then(async session => { if (!session) return console.log('no session'); const res = await fetch('https://api.kite.trade/quote?i=BFO:SENSEX2672377300CE', { headers: { 'X-Kite-Version': '3', 'Authorization': 'token ' + process.env.KITE_API_KEY + ':' + session.accessToken } }); console.log(await res.json()); });
