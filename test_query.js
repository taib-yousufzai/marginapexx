require('dotenv').config({path:'.env.local'}); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); supabase.from('instruments').select('tradingsymbol, name, expiry').or('name.ilike.nifty%,name.ilike.% nifty%,tradingsymbol.ilike.%NIFTY%').or('expiry.gte.2026-07-07,expiry.is.null').order('expiry', { ascending: true }).limit(5).then(res => console.log(res));
