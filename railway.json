{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile.ticker"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
