const fs = require('fs'); let c = fs.readFileSync('components/TradeSheet.tsx', 'utf8'); c = c.replace(/<span className="ts2-ba-bid">{fmt\(bidPrice\)}<\/span>/g, <span className=\"ts2-ba-bid\">{marketQuotes[item?.kiteSymbol]?.bid ? fmt(bidPrice) : '--'}</span>).replace(/<span className="ts2-ba-ask">{fmt\(askPrice\)}<\/span>/g, <span className=\"ts2-ba-ask\">{marketQuotes[item?.kiteSymbol]?.ask ? fmt(askPrice) : '--'}</span>); fs.writeFileSync('components/TradeSheet.tsx', c);
